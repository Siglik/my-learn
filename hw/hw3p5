import scipy.misc as spm
import math

def h(N):
    sum = 0
    for x in range(1, int(math.sqrt(N))+1):
        sum += spm.comb(N, x)
    return sum

N = input("N: ")
# m_n = h(N)
# print m_n
# print m_n/math.pow(2, N)
#
# print 1 + N + spm.comb(N, 2)
#
# print math.pow(2, int(N/2))

print 1 + spm.comb(N+1, 2)
print 1 + spm.comb(N+1, 3)